const fs=require('fs'),path=require('path');
const f=path.join(process.cwd(),'app','page.tsx');
let s=fs.readFileSync(f,'utf8');

if(!s.includes('mobileMenu,setMobileMenu')) s=s.replace('export default function Home(){','export default function Home(){\n const[mobileMenu,setMobileMenu]=useState(false);');
s=s.replace('onClick={()=>setActive(m)}>{m}</button>','onClick={()=>{setActive(m);setMobileMenu(false)}}>{m}</button>');
if(!s.includes('className={"sidebar "+(mobileMenu?"mobile-open":"")}')) s=s.replace('<aside className="sidebar">','<aside className={"sidebar "+(mobileMenu?"mobile-open":"")}><button type="button" className="mobile-close" aria-label="메뉴 닫기" onClick={()=>setMobileMenu(false)}>×</button>');
if(!s.includes('className="hamburger"')) s=s.replace('<main className="main"><header className="topbar"><h1>{active}</h1>','<main className="main"><header className="topbar"><button type="button" className="hamburger" aria-label="메뉴 열기" onClick={()=>setMobileMenu(true)}>☰</button><h1>{active}</h1>');

const submitRe=/async function submitTask\(t:any\)[\s\S]*?\n async function sendChat/;
const submitFn=`async function submitTask(t:any){const key=String(t.id);const files=submitFiles[key]||[];const title=(submitTitle[key]||"").trim();const body=(submitBody[key]||"").trim();const memo=submitMemo[key]||"";try{if(!title)throw new Error("제출 제목을 입력해주세요.");if(!body)throw new Error("제출 내용을 입력해주세요.");setBusy(true);setError("");const ids=files.length?await uploadFiles(files,setProgress):[];const result=await rpc("app_submit_task",{p_task:Number(t.id),p_memo:memo,p_files:ids,p_submission_title:title,p_submission_body:body});if(!result||result.ok===false)throw new Error(result?.message||result?.error||"숙제 제출에 실패했습니다.");setSubmitFiles(v=>({...v,[key]:[]}));setSubmitMemo(v=>({...v,[key]:""}));setSubmitTitle(v=>({...v,[key]:""}));setSubmitBody(v=>({...v,[key]:""}));setProgress("");toast("숙제가 제출되었습니다.");try{await refresh()}catch{} }catch(e:any){setProgress("");toast(e?.message||"숙제 제출에 실패했습니다.")}finally{setBusy(false)}}
 async function sendChat`;
if(submitRe.test(s)) s=s.replace(submitRe,submitFn);

const listRe=/function TaskSubmitList\([\s\S]*?\nfunction CreatedTaskList/;
const listFn=`function TaskSubmitList({tasks,me,files,setFiles,memos,setMemos,titles,setTitles,bodies,setBodies,submit,users}:{tasks:any[];me:string;files:Record<string,File[]>;setFiles:(v:Record<string,File[]>)=>void;memos:Record<string,string>;setMemos:(v:Record<string,string>)=>void;titles:Record<string,string>;setTitles:(v:Record<string,string>)=>void;bodies:Record<string,string>;setBodies:(v:Record<string,string>)=>void;submit:(t:any)=>void;users:any[]}){return <section className="card">{tasks.length?tasks.map((t:any)=>{const key=String(t.id),status=t.statusByUser?.[me]||"미제출";return <article className="task task-large" key={t.id}><div className="task-main"><div className="section-title"><div><b>{t.title}</b><div className="muted smalltext">과목: {t.subject||"-"} · 출제자: {userLabel(users.find((u:any)=>u.id===t.creatorId))} · 마감: {t.due||"-"}</div></div><span className={\`badge \${statusClass(status)}\`}>{status}</span></div><div className="task-desc">{t.desc||t.description||"내용 없음"}</div><AttachmentList attachments={t.attachments}/>{t.feedbackByUser?.[me]&&<div className="feedback">피드백: {t.feedbackByUser[me]}</div>}{status!=="완료"&&<form className="submission-form" onSubmit={e=>{e.preventDefault();submit(t)}}><div className="submission-owner"><b>제출자</b> {userLabel(users.find((u:any)=>u.id===me))}</div><input placeholder="제출 제목" value={titles[key]||""} onChange={e=>setTitles({...titles,[key]:e.target.value})} required/><textarea placeholder="제출 내용" value={bodies[key]||""} onChange={e=>setBodies({...bodies,[key]:e.target.value})} required/><textarea placeholder="제출 메모(선택)" value={memos[key]||""} onChange={e=>setMemos({...memos,[key]:e.target.value})}/><FilePicker files={files[key]||[]} setFiles={f=>setFiles({...files,[key]:f})}/><button type="submit" className="outline" disabled={!titles[key]?.trim()||!bodies[key]?.trim()}>제출하기</button></form>}</div></article>}) : <Empty text="현재 나에게 배정된 숙제가 없습니다."/>}</section>}
function CreatedTaskList`;
if(listRe.test(s)) s=s.replace(listRe,listFn);
fs.writeFileSync(f,s);

const cssFile=path.join(process.cwd(),'app','globals.css');
let css=fs.readFileSync(cssFile,'utf8');
const mobileCss=`\n/* Mobile drawer final behavior */\n@media(max-width:768px){.hamburger{display:inline-flex!important;align-items:center;justify-content:center;width:40px;height:40px;border:1px solid var(--line);border-radius:10px;background:var(--card);color:var(--fg);font-size:21px;margin-right:8px}.sidebar{position:fixed!important;left:0!important;top:0!important;bottom:0!important;width:min(82vw,320px)!important;z-index:10000!important;transform:translateX(-110%)!important;transition:transform .18s ease!important;visibility:hidden!important;pointer-events:none!important}.sidebar.mobile-open{transform:translateX(0)!important;visibility:visible!important;pointer-events:auto!important}.mobile-close{display:flex!important;align-items:center;justify-content:center;position:absolute;right:12px;top:12px;width:36px;height:36px;border:1px solid var(--line);border-radius:9px;background:var(--card);color:var(--fg);font-size:22px;z-index:10001}.main{width:100%!important;margin-left:0!important}.topbar{display:flex!important;align-items:center!important}.submission-form{width:100%;box-sizing:border-box}.submission-form input,.submission-form textarea{width:100%;box-sizing:border-box}}\n`;
if(!css.includes('Mobile drawer final behavior')){css+=mobileCss;fs.writeFileSync(cssFile,css)}
console.log('[mobile-submit-final] applied');
